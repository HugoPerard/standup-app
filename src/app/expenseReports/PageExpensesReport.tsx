import { Page, PageContent } from '@/app/layout';

export const PageExpensesReport = () => {
  return (
    <Page containerSize="full">
      <PageContent>Notes de frais | WIP</PageContent>
    </Page>
  );
};
